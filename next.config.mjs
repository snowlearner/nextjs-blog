/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['scontent.fblr1-7.fna.fbcdn.net'], // Allow images from this domain
    },
  };
  
  export default nextConfig;
  

